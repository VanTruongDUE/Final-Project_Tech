import PagePlaceholder from '../../components/common/PagePlaceholder'

export default function ResetPasswordPage() {
  return (
    <PagePlaceholder
      eyebrow="Auth"
      title="Reset Password - Dat lai mat khau"
      description="Trang dat lai mat khau hien chi la mock. Logic token reset, validate va submit se duoc tich hop o sprint tiep theo."
    />
  )
}
