
  # Edit UI components individually

  This is a code bundle for Edit UI components individually. The original project is available at https://www.figma.com/design/LVr4aZQnn3krVkkCXokbp6/Edit-UI-components-individually.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  